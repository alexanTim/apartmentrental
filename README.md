This is rental application system, you can install directly in you computer, just do the following:

make database base on the .evn
php artisan migrate 
composer install
npm install
npm run dev 
php artisan serve 
